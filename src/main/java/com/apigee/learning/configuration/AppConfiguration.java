package com.apigee.learning.configuration;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfiguration {
	/*
	 * @Bean RestTemplate restTemplate(){ return new RestTemplate(); }
	 */
}
